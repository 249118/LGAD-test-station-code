from tkinter import Y
import numpy as np
import math
import pathlib
import datetime
import pyvisa

# p = pathlib.Path("output/")
# p.mkdir(exist_ok=True)

# date = datetime.datetime.now()
# folder_name = "measurment_" + date.strftime("%d.%m.%y_%H.%M.%S")

# folder = pathlib.Path("output/" + folder_name + "/")
# folder.mkdir(exist_ok=False)

# for x in range(1,4):
#     file_name = "Voltage"+str(100*x)+"V.csv"
#     file_path = folder / file_name
#     with file_path.open("a") as f:
#         for i in range(1, 10):
#             f.write("1,2,3\n")
rm = pyvisa.ResourceManager()
oscilloscope_ref = rm.open_resource("TCPIP0::WINDOWS-V73KLVE.local::inst0::INSTR")
oscilloscope_ref.read_termination = '\n'
oscilloscope_ref.write_termination = '\n'
oscilloscope_ref.timeout = 1000

while True:


